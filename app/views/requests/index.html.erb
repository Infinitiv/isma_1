<h2><span class="headline2">Перечень заявок<span></h2>

<table>
  <tr>
    <th>User</th>
    <th>Department</th>
    <th>Description</th>
    <th>Comment</th>
    <th>Status</th>
    <th>Priority</th>
    <th>Master</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @requests.each do |request| %>
  <tr>
    <td><%= request.user_id %></td>
    <td><%= request.department_id %></td>
    <td><%= request.description %></td>
    <td><%= request.comment %></td>
    <td><%= request.status_id %></td>
    <td><%= request.priority_id %></td>
    <td><%= request.master_id %></td>
    <td><%= link_to 'Show', request %></td>
    <td><%= link_to 'Edit', edit_request_path(request) %></td>
    <td><%= link_to 'Destroy', request, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Request', new_request_path %>
